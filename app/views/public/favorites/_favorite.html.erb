<% if recipe.favorited_by?(current_user) %>
  <%= link_to "お気に入り登録解除", recipe_favorites_path(recipe.id), method: :delete, remote: true %>
<% else %>
  <%= link_to "お気に入り登録", recipe_favorites_path(recipe.id), method: :post, remote: true %>
<% end %>